node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
.env
.env.local
.env.*.local
*.log
*.tsbuildinfo

# Single-user JSON data store (todos / note / dismiss state). Override location
# with AURORA_DATA_DIR; point it at a synced folder to persist across machines.
data/

# Python backend
**/.venv/
__pycache__/
*.egg-info/
.pytest_cache/
